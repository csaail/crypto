```markdown
# Crypto Stego Tool

A simple tool for steganography and cryptography.

## Installation

Run the following commands:

```bash
pip install stegano
pip install pycryptodome
pip install bcrypt
